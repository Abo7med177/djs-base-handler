const { model, Schema } = require("mongoose");
module.exports = model("m5alfat", new Schema({
    userId: String, 
    guildId: String,
m5alfh: String,
id: Number,
timestamp: Number,
userm: String, 
s3r: Number
})
                      )